/**
 * Applies configuration for creating a release.
 */

apply plugin: 'net.researchgate.release'

release {
    failOnSnapshotDependencies = false
    tagTemplate = '$name-$version'
}

project.afterEvaluate {
    afterReleaseBuild.dependsOn uploadShadow
}
